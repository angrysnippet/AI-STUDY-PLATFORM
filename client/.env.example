# URL of the backend server.
VITE_API_URL=http://localhost:4000

# Google sign-in is driven by the SERVER's config (GET /api/auth/config). You do
# not need to set a client id here — the client receives it from the server.
